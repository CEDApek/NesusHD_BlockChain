# 软件工程测试报告：基于区块链的资源共享系统

## 1. 引言

### 1.1 编写目的
简要说明本报告用于评估基于区块链的资源共享原型系统是否满足需求、设计目标、功能期望与非功能质量要求，并用于评价开发实现质量、测试覆盖情况和测试阶段遗留风险。

### 1.2 背景
介绍资源共享平台的应用背景，以及文件归属追溯、文件完整性、下载/支付记录、奖励公平性、交易历史透明性等常见问题。说明本仓库通过 Vue 3 前端、Flask REST API 和模拟账本模块来实现上传、下载、挖矿奖励和区块查询等区块链式行为。

### 1.3 定义
列出并解释报告中使用的术语：功能测试、边界测试、压力测试、界面/UI 测试、单元测试、集成测试、区块链账本、交易、挖矿/奖励确认、文件哈希/完整性验证等。

### 1.4 参考资料
列出编写报告时参考的资料，例如仓库源代码、README、后端 API 实现、`hyperledger/ledger.py`、前端 Vue 组件、Flask 文档、Vue/Vite 文档以及课程软件工程测试资料。

## 2. 测试概述
概述总体测试策略与测试范围。包含列名为 `测试项目`、`测试目的`、`测试内容` 的表格，覆盖单元/模块测试、模块结构测试、功能验证测试、边界测试、压力测试和用户界面测试。

## 3. 面向对象 / 模块结构测试

### 3.1 系统总体架构
说明仓库实际架构：`frontend/` 为 Vue 3 单页应用，`backend/app.py` 为 Flask 路由，`hyperledger/ledger.py` 为模拟账本类，`backend/uploads/` 为本地上传文件目录。加入 Mermaid 架构图。

### 3.2 具体模块测试
说明模块测试先验证各模块独立正确性，再验证前端组件、REST 接口、文件存储和账本状态之间的交互。

#### 3.2.1 单元测试
为认证、文件上传、搜索下载、交易奖励、区块账本和状态管理等模块建立单元测试表。表格列为 `测试内容`、`测试结果`、`备注`。

#### 3.2.2 模块一致性 / CRC 风格测试
建立 CRC 风格的模块职责表。每个模块包含模块名称、编号、描述、职责/功能和协作模块，并说明 UI 状态、REST 响应、文件元数据、上传哈希、交易记录与区块之间的一致性检查。

## 4. 功能验证测试
采用黑盒测试通过 REST API 和 Vue 界面验证系统可见行为。设置登录注册、资源发布上传、资源搜索下载、交易奖励、区块链账本查询、系统/管理/数据管理等小节。表格列为 `功能名称`、`输入`、`预期输出`、`实际输出`。

## 5. 边界测试
说明通过边界输入发现健壮性问题。设置登录注册、上传、搜索下载、交易挖矿、区块链数据校验等小节。表格列为 `功能名称`、`边界输入`、`预期输出`、`实际输出`。

## 6. 压力测试
说明采用非生产的模拟数据进行压力测试。包含测试环境、并发用户数、平均响应时间、成功率、CPU 占用、内存占用和瓶颈观察。至少包含一个 Markdown 表格和一个 Mermaid `xychart-beta` 响应时间图。

## 7. 用户界面测试
说明对主要 Vue 页面/组件的 UI 测试：登录、注册、首页/资源列表、上传、资源详情/下载、挖矿奖励、区块查询/账本。表格列为 `功能名称`、`预期操作`、`实际操作`。

## 8. 软件功能结论
分别总结登录注册、资源管理、交易奖励、区块链账本和用户界面模块的测试结论，标明功能正常、部分完成或仍需改进。

## 9. 分析总结

### 9.1 能力
总结当前原型系统完成较好的能力，包括账号处理、文件发布、重复检测、资源列表、下载次数限制、奖励模拟和区块查询等。

### 9.2 局限性
总结测试发现的局限，例如本地模拟而非真实分布式区块链节点、压力测试环境有限、状态以内存为主、挖矿奖励规则简化、安全权限控制不足以及 UI/错误提示仍可优化。

## 10. 测试资源消耗
说明测试所使用的资源：本地开发 PC、浏览器、Python/Flask 环境、Node.js/Vite 环境、后端服务、前端开发服务器、本地上传存储和模拟并发 HTTP 请求。

