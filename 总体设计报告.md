# 总体设计报告

Project Name: Nexus BT资源共享与区块链激励平台（规划版）

## 1 体系结构设计

### 1.1 目的
本总体设计将用于指导“Nexus BT资源共享与区块链激励平台”的后续工程化建设过程。项目将以“资源共享 + 区块链激励 + 可追溯记录”为核心目标，形成可扩展、可维护、可演进的系统架构。体系结构设计将会在后续阶段支撑模块边界划分、接口协议约束、数据模型标准化以及运行维护策略制定。

具体而言，本设计将计划实现以下作用：
- 为前后端与账本子系统定义清晰职责边界，降低耦合度；
- 为REST API、文件处理接口、挖矿/记账流程提供统一设计基线；
- 为后续接入真实Hyperledger Fabric网络预留替换层；
- 为权限控制、日志追踪与运行监控提供结构性支持。

### 1.2 体系结构风格
本系统拟采用“分层体系结构 + 前后端分离架构 + RESTful架构 + 数据中心型（账本中心）”的组合风格。

1. 分层体系结构：后端将分为API接口层、业务逻辑层、账本服务层、数据/文件访问层，以增强可维护性与可测试性。  
2. 前后端分离架构：Vue客户端将通过HTTP接口调用Flask服务，后续将便于独立部署、版本迭代与跨端扩展。  
3. RESTful架构：系统接口将围绕用户、文件、区块、奖励等资源建模，预计提升接口一致性。  
4. 数据中心型（账本中心）：资源声明、下载奖励、区块记录将围绕统一账本状态组织，便于追溯与审计。

该组合将适配当前项目的“交互展示 + 业务编排 + 账本模拟”特征，并为后续真实区块链替换提供连续演进路径。

### 1.3 层级结构
系统计划采用如下层级：
- 顶层：Nexus平台整体系统；
- 次顶层：前端交互子系统、后端服务子系统、账本子系统；
- 中间层：认证与用户、资源文件管理、区块挖矿与奖励、分类与检索、下载控制与审计；
- 底层：本地文件存储、内存账本结构、Flask/Vue运行环境、第三方依赖（axios、flask-cors等）。

```mermaid
graph TD
 A[Nexus BT资源共享与区块链激励平台] --> B[前端交互子系统 Vue]
 A --> C[后端服务子系统 Flask]
 A --> D[账本子系统 Hyperledger Mock]
 B --> E[登录与会话视图模块]
 B --> F[文件列表/详情/上传模块]
 B --> G[区块浏览与筛选模块]
 C --> H[认证与用户API]
 C --> I[文件管理API]
 C --> J[账本与挖矿API]
 D --> K[用户身份与地址管理]
 D --> L[交易池与区块链记录]
 D --> M[资源元数据管理]
 I --> N[底层支持: uploads目录/哈希工具]
 J --> O[底层支持: 内存状态/时间戳/日志]
```

### 1.4 体系结构环境图和原型
系统将处于“用户—Web客户端—后端服务—账本/存储”环境中运行。外部参与者主要包括普通成员与管理员；系统内部将由前端展示、后端API和账本服务协同组成；底层将依赖Python运行时、Node运行时、文件系统以及后续可替换的区块链网络接口。

```mermaid
graph LR
    User[普通用户/管理员] --> Web[Web前端 Vue SPA]
    Web --> API[后端API Flask]
    API --> Ledger[账本服务 ResourceSharingSystem]
    API --> Store[(文件存储 uploads)]
    API --> Runtime[运行环境 Python/Node]
    Ledger --> Chain[(区块数据/交易池 内存态)]
    API --> Ext[后续外部接口: Fabric SDK/CA]
```

#### 1.4.1 顶层 - 系统整体设计
- 模块职责：系统整体将提供资源共享、信用激励、区块追踪的一体化服务。  
- 主要输入：用户登录信息、资源文件、检索条件、挖矿请求。  
- 主要输出：认证结果、文件目录、下载流、账户财富与区块记录。  
- 依赖关系：依赖前端交互、后端API编排、账本服务状态一致性。  
- 后续实现计划：后续将把“模拟账本”逐步替换为“真实Fabric网络调用层”。

#### 1.4.2 次顶层 - 前端交互子系统设计
- 模块职责：负责登录、仪表盘、文件浏览、上传交互、区块筛选展示。  
- 主要输入：用户操作事件、API返回JSON。  
- 主要输出：页面状态、可视化列表、交互反馈与错误提示。  
- 依赖关系：依赖后端 `/api/login`、`/api/files`、`/api/blocks` 等接口。  
- 后续实现计划：将计划完善状态管理、表单校验一致性与多角色可视化策略。

#### 1.4.3 次顶层 - 后端服务子系统设计
- 模块职责：负责请求接入、参数验证、业务规则执行、文件读写、账本调用。  
- 主要输入：HTTP请求（JSON/Multipart）。  
- 主要输出：标准化JSON响应与文件流响应。  
- 依赖关系：依赖账本服务对象、哈希计算、下载限制计数器、本地存储目录。  
- 后续实现计划：后续将引入更完善的令牌认证、异常分层与可观测日志。

#### 1.4.4 中间层 - 认证与用户模块设计
认证与用户模块将会部署在中间层，承担“前端身份交互”与“后端授权执行”之间的枢纽职责，并向下连接账本身份映射能力。该模块在总体架构中的目标，是将登录、注册、角色识别与账本身份绑定统一纳入标准化认证链路，确保用户在界面层的身份状态与账本层的身份标识保持一致。后续在多角色、多终端访问场景中，本模块将作为统一身份入口，支撑系统的访问边界管理与会话治理。

本模块将负责用户注册、登录、凭据校验、角色判定、会话/令牌生成以及账本身份映射。为提高可维护性，系统计划采用“认证处理—权限解析—身份映射”分段式处理方式：先验证凭据合法性，再解析角色权限，最后完成链上或账本身份绑定。通过该分段设计，业务模块后续将可复用统一认证结果，减少重复鉴权逻辑并降低耦合风险。

输入方面，本模块将接收用户名、密码、登录会话信息、角色请求参数与注册资料；输出方面，本模块将返回令牌、角色信息、用户画像、账本身份标识以及认证失败消息。后续将计划统一错误码与失败语义（如凭据错误、权限不足、身份映射异常），以支持前端进行精确提示和流程回退控制。

依赖关系方面，本模块将依赖凭据存储结构、密码加密组件、Token管理策略、角色配置规则和账本用户注册接口。后续实现计划将包括：持久化用户库、加密口令存储、令牌过期与刷新机制、RBAC访问控制、以及更安全的账本身份绑定策略，并逐步引入会话吊销和异常登录防护能力。

认证与用户模块环境图（参考网络模块风格）：
```mermaid
graph TD
 U[用户/管理员] --> F[前端登录注册界面]
 F --> A[认证与用户模块]
 A --> C[凭据存储结构]
 A --> R[角色配置与权限规则]
 A --> L[账本用户注册接口]
 A --> T[Token生成与会话管理]
 A --> O[返回Token、角色与账本身份]
```

认证与用户模块处理流程图：
```mermaid
flowchart TD
    A[接收登录或注册请求] --> B[解析用户名与密码]
    B --> C[校验输入格式]
    C --> D[查询或创建用户凭据]
    D --> E[进行密码校验或加密存储]
    E --> F[判定用户角色]
    F --> G[确保账本身份映射]
    G --> H[生成Token与用户画像]
    H --> I[返回认证结果]
```

认证与用户模块原型关系图（参考“控制-数据流”抽象）：
```mermaid
graph LR
    A[凭据输入] --> B[凭据校验器]
    B --> C[角色解析器]
    C --> D[账本身份映射器]
    D --> E[令牌管理器]
    E --> F[认证响应]
```

#### 1.4.5 中间层 - 资源文件管理模块设计
资源文件管理模块将位于中间层资源治理位置，连接前端资源交互界面、后端服务编排层、底层文件存储与账本资源管理器。本模块将会作为资源生命周期的统一入口，覆盖资源提交、校验、登记、查询、下载与审计触发等关键过程。通过统一入口设计，系统将计划在资源一致性、可追溯性与治理可控性之间取得平衡。

本模块将负责文件上传、元数据提取、分类管理、哈希查重、详情查询、下载权限检查与下载响应生成。系统拟采用“上传链路与下载链路分治”策略：上传链路强调完整性、唯一性与可登记性；下载链路强调权限合法性、文件存在性和行为可记录性。该设计将为后续奖励模块提供稳定事件来源，并降低跨模块耦合复杂度。

输入将包括上传文件流、文件名、文件类型、业务元数据、查询筛选条件与下载请求；输出将包括资源目录列表、详情对象、查重结果、下载二进制流与校验异常信息。为适配后续扩展，系统将计划支持分页、分类、发布者与时间维度查询，并在下载响应中附加可审计上下文。

依赖关系方面，本模块将依赖哈希计算组件、上传目录/对象存储、文件元数据模型、账本资源管理器、认证与权限模块及存储适配器。后续实现计划将扩展对象存储支持、异步扫描、访问审计、限流控制与更细粒度类型校验，同时引入可配置策略中心，提升高并发与大规模文件场景下的稳定性。

资源文件管理模块环境图（参考数据库模块风格）：
```mermaid
graph TD
    U[用户/管理员] --> F[资源上传与查询界面]
    F --> M[资源文件管理模块]
    M --> H[哈希计算组件]
    M --> S[(上传目录/对象存储)]
    M --> D[文件元数据结构]
    M --> L[账本资源管理器]
    M --> A[认证与权限模块]
    M --> O[文件列表、详情或下载流]
```

上传与查重流程图：
```mermaid
flowchart TD
    A[接收上传文件流] --> B[读取文件元数据]
    B --> C[计算文件哈希值]
    C --> D{是否存在重复文件}
    D -- 是 --> E[返回重复校验提示]
    D -- 否 --> F[保存文件到上传目录或对象存储]
    F --> G[写入资源元数据]
    G --> H[登记账本资源信息]
    H --> I[返回上传结果]
```

查询与下载流程图：
```mermaid
flowchart TD
    A[接收资源查询或下载请求] --> B[校验用户身份与权限]
    B --> C[解析筛选条件或资源ID]
    C --> D[查询资源元数据]
    D --> E{是否为下载请求}
    E -- 否 --> F[返回资源列表或详情]
    E -- 是 --> G[检查下载权限与文件存在性]
    G --> H[生成下载二进制流]
    H --> I[记录下载事件或触发奖励]
    I --> J[返回下载响应]
```

资源文件管理模块原型关系图：
```mermaid
graph LR
    A[文件输入] --> B[元数据提取器]
    B --> C[哈希校验器]
    C --> D[存储适配器]
    D --> E[账本资源管理器]
    E --> F[资源响应]
```

#### 1.4.6 中间层 - 区块挖矿与奖励模块设计
区块挖矿与奖励模块将会定位为中间层的结算中枢，连接资源下载事件、待确认交易池、管理员挖矿操作与账本链结构。本模块将把业务行为转化为可确认交易，并通过挖矿确认流程形成区块元数据与奖励结果，从而为平台激励机制提供可追踪、可审计、可解释的结算路径。后续系统演进中，本模块将成为链上治理与激励策略迭代的关键承载点。

本模块将负责待确认交易汇聚、挖矿触发处理、奖励计算、区块检索、区块导出与财富更新。系统拟采用“事件入池—挖矿确认”两阶段模型：第一阶段记录下载等业务触发事件并写入待确认池；第二阶段由管理员触发挖矿完成区块生成与奖励结算。该设计将计划在响应效率与账本一致性之间形成可控平衡。

输入将包括挖矿请求、待确认交易列表、区块筛选条件、下载奖励事件与管理员操作信息；输出将包括区块元数据、挖矿结果、奖励结果、财富变更结果与待处理交易统计。后续将规划统一查询接口，支持按区块号、矿工标识、哈希片段和时间范围检索，并为导出与报表场景提供标准结果结构。

依赖方面，本模块将依赖账本链结构、待确认交易池、时间戳格式化能力、管理员权限控制、资源下载事件与未来链码接口。后续实现计划将对接真实链码调用、补充链上事件订阅、优化奖励规则、增强区块导出能力并完善审计追踪，以支撑跨节点协同与治理策略持续优化。

区块挖矿与奖励模块环境图（参考编码模块风格）：
```mermaid
graph TD
    Admin[管理员] --> UI[区块管理/挖矿操作界面]
    User[普通用户] --> R[资源下载事件]
    UI --> M[区块挖矿与奖励模块]
    R --> M
    M --> P[待确认交易池]
    M --> C[账本链结构]
    M --> W[财富/奖励记录]
    M --> A[管理员权限控制]
    M --> E[区块导出接口]
    M --> O[区块元数据与奖励结果]
```

挖矿处理流程图：
```mermaid
flowchart TD
    A[接收管理员挖矿请求] --> B[校验管理员权限]
    B --> C[读取待确认交易]
    C --> D{是否存在待处理交易}
    D -- 否 --> E[返回无待处理交易提示]
    D -- 是 --> F[计算区块元数据]
    F --> G[生成新区块]
    G --> H[更新账本链结构]
    H --> I[结算挖矿奖励]
    I --> J[清空或更新待确认交易池]
    J --> K[返回挖矿结果]
```

下载奖励触发流程图：
```mermaid
flowchart TD
    A[用户触发资源下载] --> B[资源文件管理模块完成权限校验]
    B --> C[生成下载事件]
    C --> D[区块挖矿与奖励模块接收事件]
    D --> E[计算资源贡献或下载奖励]
    E --> F[写入待确认交易]
    F --> G[更新用户财富变更记录]
    G --> H[等待后续挖矿确认]
```

区块挖矿与奖励模块原型关系图：
```mermaid
graph LR
    A[待确认交易] --> B[挖矿控制器]
    B --> C[区块生成器]
    C --> D[奖励计算器]
    D --> E[账本更新器]
    E --> F[区块/奖励结果]
```

## 2 服务器端设计

### 2.1 服务器架构
服务器端将采用分层架构，预计包含：
- 表示层/API接口层：提供登录、文件、区块、奖励等REST端点；
- 业务逻辑层：执行权限判定、查重规则、下载次数限制、奖励分配；
- 服务层：封装账本操作（用户注册、交易记录、区块查询）；
- 数据访问层：负责上传文件目录访问、哈希读写、内存结构读写；
- 存储层：本地文件系统 + 账本内存态（后续可替换Fabric）；
- 外部接口层：后续将接入Fabric SDK、CA服务与链码接口。

```mermaid
graph TD
    A[表示层 / API接口层 Flask Routes] --> B[业务逻辑层 规则校验]
    B --> C[服务层 Ledger Service]
    B --> D[数据访问层 文件与计数器]
    C --> E[(账本存储: 内存链状态)]
    D --> F[(文件存储: backend/uploads)]
    C --> G[外部接口层: 预留Fabric SDK]
```

### 2.2 服务器处理流程
系统后续将采用统一请求处理流程：
1. 用户或前端提交HTTP请求；
2. API层接收并路由至目标控制器；
3. 进行参数校验与格式规范化；
4. 执行权限与作用域检查（如管理员可见范围）；
5. 执行业务逻辑（查重、下载限次、奖励计算等）；
6. 调用账本服务与文件存储；
7. 生成结果并映射为统一响应结构；
8. 返回前端并触发界面刷新；
9. 记录日志与必要的审计信息。

```mermaid
flowchart TD
    A[用户发送请求] --> B[Flask API接收]
    B --> C[参数解析与校验]
    C --> D[权限与范围检查]
    D --> E[业务处理]
    E --> F[访问账本/文件存储]
    F --> G[生成响应数据]
    G --> H[返回客户端]
    H --> I[日志与审计记录]
```

### 2.3 数据库设计 / 数据存储设计
鉴于当前项目特征，系统后续将采用“关系化数据模型设计 + 文件存储”的混合方案。即使原型阶段以内存结构实现，设计阶段仍将先给出标准实体模型，以支持后续迁移到MySQL/PostgreSQL或链上状态数据库。

```mermaid
erDiagram
    USER ||--o{ FILE_RESOURCE : uploads
    USER ||--o{ BLOCK : mines
    FILE_RESOURCE ||--o{ DOWNLOAD_LOG : downloaded_by
    USER ||--o{ DOWNLOAD_LOG : performs
    BLOCK ||--o{ TRANSACTION : contains
    USER ||--o{ WEALTH_LEDGER : owns
```

系统后续将设计如下核心表（或等价模型）：

| 表名/模型名 | 字段名 | 数据类型 | 字段说明 | 主键 | 必填 |
|---|---|---|---|---|---|
| user_account | id | BIGINT | 用户唯一标识 | 是 | 是 |
| user_account | username | VARCHAR(64) | 登录名，将用于身份识别 | 否 | 是 |
| user_account | password_hash | VARCHAR(255) | 密码摘要，将用于认证校验 | 否 | 是 |
| user_account | role | VARCHAR(32) | 角色（administrator/member） | 否 | 是 |
| user_account | ledger_address | VARCHAR(128) | 账本地址映射 | 否 | 否 |
| file_resource | id | BIGINT | 资源ID | 是 | 是 |
| file_resource | owner_id | BIGINT | 所属用户ID | 否 | 是 |
| file_resource | file_name | VARCHAR(255) | 文件名称 | 否 | 是 |
| file_resource | file_hash | CHAR(64) | 文件哈希，将用于查重 | 否 | 是 |
| file_resource | category | VARCHAR(32) | 分类标签 | 否 | 是 |
| file_resource | size_bytes | BIGINT | 文件大小（字节） | 否 | 是 |
| file_resource | storage_path | VARCHAR(512) | 存储路径 | 否 | 是 |
| download_log | id | BIGINT | 下载日志ID | 是 | 是 |
| download_log | file_id | BIGINT | 被下载资源ID | 否 | 是 |
| download_log | downloader_id | BIGINT | 下载者ID | 否 | 是 |
| download_log | attempt_no | INT | 当前用户对资源的下载次数 | 否 | 是 |
| download_log | downloaded_at | DATETIME | 下载时间戳 | 否 | 是 |
| block_record | id | BIGINT | 区块记录ID | 是 | 是 |
| block_record | block_index | INT | 区块高度 | 否 | 是 |
| block_record | block_hash | CHAR(64) | 区块哈希 | 否 | 是 |
| block_record | previous_hash | CHAR(64) | 前序哈希 | 否 | 否 |
| block_record | miner_user_id | BIGINT | 记账者用户ID | 否 | 否 |
| block_record | tx_count | INT | 交易数量 | 否 | 是 |
| wealth_ledger | id | BIGINT | 财富台账ID | 是 | 是 |
| wealth_ledger | user_id | BIGINT | 用户ID | 否 | 是 |
| wealth_ledger | wealth_value | DECIMAL(18,2) | 当前积分/财富值 | 否 | 是 |
| wealth_ledger | pending_tx | INT | 待确认交易数 | 否 | 是 |
| wealth_ledger | updated_at | DATETIME | 更新时间 | 否 | 是 |

## 3 UI 设计

### 3.1 设计原则
本系统UI（含Web交互与API交互）将遵循以下原则：
- 简洁明了：页面层级将控制在最少必要深度，降低学习成本；
- 一致性：按钮、状态标签、错误提示与术语将统一；
- 易用性：关键流程（登录、上传、下载、挖矿）将减少操作步骤；
- 可维护性：组件化结构将支持后续独立迭代；
- 安全性：将采用输入校验、权限提示、敏感信息遮蔽；
- 信息反馈清晰：加载中、成功、失败、限制条件将实时反馈；
- 操作流程清楚：通过导航分区呈现“文件—上传—区块”主流程。

### 3.2 UI 原型设计 / 交互原型设计
系统将规划Web前端原型，并辅以API交互原型。

```mermaid
flowchart TD
    A[用户进入系统] --> B[登录/注册]
    B --> C[进入仪表盘]
    C --> D[浏览社区文件]
    C --> E[提交资源上传]
    C --> F[查看区块与筛选]
    D --> G[查看详情并下载]
    E --> H[等待资源入账]
    F --> I[触发挖矿并刷新财富]
    G --> I
    H --> I
```

**页面/交互原型规划**

1. 登录与注册页  
   - 页面功能：用户身份接入与基础账户创建。  
   - 主要输入：用户名、密码。  
   - 主要输出：登录令牌、角色、账本身份。  
   - 用户操作流程：填写凭据 → 提交校验 → 成功后进入仪表盘。

2. 仪表盘总览区  
   - 页面功能：展示个人财富、待确认交易、角色信息、身份可见性切换。  
   - 主要输入：刷新操作、挖矿触发操作。  
   - 主要输出：财富数值变化、挖矿过程状态反馈。  
   - 用户操作流程：查看状态 → 执行挖矿 → 接收结果反馈。

3. 社区文件页（列表+详情）  
   - 页面功能：资源检索、筛选、详情查看与下载。  
   - 主要输入：分类筛选条件、详情查看动作、下载动作。  
   - 主要输出：文件列表、详情信息、下载结果。  
   - 用户操作流程：筛选列表 → 查看详情 → 发起下载。

4. 上传页  
   - 页面功能：提交本地文件与元数据并请求入账。  
   - 主要输入：文件、类别、描述等元信息。  
   - 主要输出：上传成功/失败信息、查重与大小限制反馈。  
   - 用户操作流程：选择文件 → 填写元数据 → 提交并等待响应。

5. 区块浏览页  
   - 页面功能：展示区块记录并提供搜索筛选（管理员全量，成员个人）。  
   - 主要输入：关键字、区块号、矿工筛选条件。  
   - 主要输出：区块列表、元数据、可导出结果（后续）。  
   - 用户操作流程：设置筛选 → 查询区块 → 查看记录/导出。

6. API交互原型（配套）  
   - 交互功能：前端将通过 `/api/login`、`/api/files`、`/api/files/{owner}/{id}`、`/api/files/{owner}/{id}/download`、`/api/ledger/reward`、`/api/blocks` 等接口完成业务闭环。  
   - 输入输出：JSON请求/响应 + 文件流下载。  
   - 流程说明：客户端发起请求 → 服务端校验处理 → 返回结构化结果并驱动UI状态更新。
